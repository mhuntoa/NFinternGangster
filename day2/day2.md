####Day 2
```
git clone <url>
git push -u origin <branch name> //-u means remember url
git pull (fetch + merge)
git remote add origin <url> //use url as origin
git branch <branch name> //create new branch
git checkout -b <branch> C2<node> //create new branch and checkout new branch
git diff <file name> //compare files
git fetch
git merge <branch>
git tag <version> //can't move tag
git push --tags //push tag only
```
**React**

- get started
- Hello World !
